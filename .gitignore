# Local secrets / env (never commit real keys)
.env
.env.*
!.env.example
*.env.local
**/email.env
**/web.env
**/*.pem
**/*.key
# Nova V2 local vector store (Chroma)
.chroma/

# Editor / OS
.DS_Store
*.swp
*~
